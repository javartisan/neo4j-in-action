{
  "name": "neo4j-in-action",
  "version": "1.0.0",
  "private": true,
  "description": "证券行业知识图谱 B/S 系统",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend",
    "docs:build": "npm install --prefix md && npm run build --prefix md",
    "docs:preview": "npx --yes serve md/site"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
